# Brand fonts (IBM Plex)

SIL Open Font License — see `OFL-IBM-Plex.txt`.

Used for CI PDFs and brand documents. Source: Google Fonts / IBM Plex.

Regenerate static Sans weights from the variable font if needed:

```bash
# see scripts/generate-fragebogen-pdf.py — fonts must exist before PDF build
```
